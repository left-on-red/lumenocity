class Collider {
    constructor(x, y, w, h) {
        this.x = x;
        this.y = y;
        this.w = w;
        this.h = h;
    }

    /**
     * compares 2 colliders and returns a boolean representing whether or not they are colliding with each other
     * @param {Collider} collider 
     */
    collides(collider) {
        
    }
}

module.exports = Collider;